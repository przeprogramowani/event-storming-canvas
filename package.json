{
  "name": "event-storming-canvas",
  "version": "1.1.0",
  "private": true,
  "description": "Local Event Storming workshops with revision-safe human and agent collaboration",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/store.js && node --check public/model.js && node --check public/sync.js && node --check public/app.js && node --check scripts/board.js && node --check public/layout.js",
    "test": "node --test test/*.test.js",
    "verify": "npm run check && npm test",
    "board": "node scripts/board.js",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
